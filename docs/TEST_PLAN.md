# V0.8 test plan

- Upgrade from a V0.5 configuration and verify lists, history and active session remain available.
- Add items and use the add-and-quote button.
- Import plain text, CSV and JSON in append, replace and create-new modes.
- Verify unresolved item names are reported without blocking valid rows.
- Compare all four data centers and open each route from its quote card.
- Start a purchase session directly from a quote card and directly from the route page.
- Complete the current world and confirm the current index advances.
- Refresh remaining session items and apply the new route manually.
- Verify quote history trend, risk price, stale-data count and target-price state.
- Reload and unload the plugin while a refresh and a purchase session exist.
